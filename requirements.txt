# Fogged — Python dependencies
opencv-python==4.10.0.84   # webcam, blur, compositing, display (5.x has broken GUI on Windows)
numpy                      # the fog-mask math
mediapipe==0.10.14         # hand tracking. NEWER VERSIONS REMOVED the legacy `solutions` API — pin this.
sounddevice                # microphone -> breath detection
