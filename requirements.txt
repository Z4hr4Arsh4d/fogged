# Fogged — Python dependencies
opencv-python      # webcam capture, blur, compositing, display
numpy              # the fog-mask math
mediapipe          # real-time hand / fingertip tracking  (M2)
# sounddevice      # microphone -> breath detection        (M3, uncomment then)
